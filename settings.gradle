// Cannot convert to kts yet for some reason, IDEA doesn't recognize it
rootProject.name = "net.twisterrob.gradle"

include(":quality")
include(":common")
include(":test")

[ "checkstyle", "pmd", "detekt" ].each {
	include(":${it}")
	project(":${it}").projectDir = file("checkers/${it}")
}
